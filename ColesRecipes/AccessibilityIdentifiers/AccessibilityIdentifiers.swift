//
//  AccessibilityIdentifiers.swift
//  ColesRecipes
//

import Foundation

enum AccessibilityIdentifiers {
    enum List {
        enum Card {
            static let image = "AI.List.Card.Image"
            static let title = "AI.List.Card.Title"
            static let detail = "AI.List.Card.Detail"
        }
    }

    enum Recipe {
        static let scrollView = "AI.Recipe.ScrollView"
        static let title = "AI.Recipe.Title"
        static let description = "AI.Recipe.Description"
        static let ingredients = "AI.Recipe.Ingredients"
    }
}
